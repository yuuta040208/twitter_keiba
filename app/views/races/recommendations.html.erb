<div class="row">
  <div class="col-sm-6">
    <% if @user_collection.win_kenjitsu_user_stat.present? %>
      <% win_kenjitsu_tweet = @user_collection.win_kenjitsu_user_stat.user.tweets.find_by(race_id: @race.id) %>
      <%= render partial: 'tweet', locals: {tweet: win_kenjitsu_tweet, user_name_text: '（堅実型・単勝）'} %>
    <% else %>
      堅実型・単勝 のおすすめツイートはありません。
    <% end %>
  </div>

  <div class="col-sm-6">
    <% if @user_collection.place_kenjitsu_user_stat.present? %>
      <% place_kenjitsu_tweet = @user_collection.place_kenjitsu_user_stat.user.tweets.find_by(race_id: @race.id) %>
      <%= render partial: 'tweet', locals: {tweet: place_kenjitsu_tweet, user_name_text: '（堅実型・複勝）'} %>
    <% else %>
      堅実型・複勝 のおすすめツイートはありません。
    <% end %>
  </div>
</div>

<div class="row mt-3">
  <div class="col-sm-6">
    <% if @user_collection.win_ippatsu_user_stat.present? %>
      <% win_ippatsu_tweet = @user_collection.win_ippatsu_user_stat.user.tweets.find_by(race_id: @race.id) %>
      <%= render partial: 'tweet', locals: {tweet: win_ippatsu_tweet, user_name_text: '（一発型・単勝）'} %>
    <% else %>
      一発型・単勝 のおすすめツイートはありません。
    <% end %>
  </div>

  <div class="col-sm-6">
    <% if @user_collection.place_ippatsu_user_stat.present? %>
      <% place_ippatsu_tweet = @user_collection.place_ippatsu_user_stat.user.tweets.find_by(race_id: @race.id) %>
      <%= render partial: 'tweet', locals: {tweet: place_ippatsu_tweet, user_name_text: '（一発型・複勝）'} %>
    <% else %>
      一発型・単勝 のおすすめツイートはありません。
    <% end %>
  </div>
</div>